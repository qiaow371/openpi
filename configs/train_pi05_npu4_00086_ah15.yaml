# π0.5 4×Ascend910 NPU 8-11 — 00086 抽屉/药盒，chunk/AH=15，其余走推荐档
# 128887 帧 × 100 ep / bs64 ⇒ 2013 步/ep × 100 = 201300 步
config_name: pi05_aloha
framework: pytorch

exp_name: 00086_PI05_npu4_bs64_lr3.5e-5_ah15_fixbase
project_name: openpi
checkpoint_base_dir: ./checkpoints
assets_base_dir: ./assets

batch_size: 64
num_epochs: 100
seed: 42
num_workers: 8
log_interval: 50

save_every_epochs: null
save_interval: 500
save_full_every_epochs: null
keep_period: 5000
overwrite: true
resume: false
wandb_enabled: false
fsdp_devices: 4
ema_decay: 0.99

pytorch_training_precision: bfloat16
pytorch_weight_path: /home/pi05/models

model:
  action_horizon: 15

lr_schedule:
  warmup_steps: 2000
  peak_lr: 3.5e-5
  decay_lr: 3.5e-6
  decay_steps: 201300

optimizer:
  b1: 0.9
  b2: 0.95
  eps: 1.0e-8
  weight_decay: 1.0e-10
  clip_gradient_norm: 1.0

data:
  repo_id: /home/pi05/00086_20260827_RM_01_YJ_ZL_21
  assets:
    assets_dir: /home/pi05/00086_20260827_RM_01_YJ_ZL_21
    asset_id: "."
  default_prompt: "Using both hands, pull open the drawer with the blue handle using your left hand, take out the white medicine box with your right hand, place it into the green drawer, and finally close the drawer."
  prompt_from_task: true
  prompt_from_subtask: false
  adapt_to_pi: false
  delta_action_dims: [7, 7, -1, -1]
