# 00070 agx01_gzy / LeRobot v2.1 / 江算 PPU PyTorch π0.5
# 14D 双臂 (7+7), 197 episodes, 42372 frames

config_name: pi05_aloha
framework: pytorch

exp_name: 00070_PI05_bs32_chunk50_agx01_gzy
project_name: openpi
checkpoint_base_dir: /nvme2n1/outputs
assets_base_dir: ./assets

batch_size: 32
num_epochs: 100
seed: 42
num_workers: 0
log_interval: 100
save_every_epochs: 1
keep_period: 5000
overwrite: true
resume: false
wandb_enabled: false
fsdp_devices: 1
ema_decay: 0.99

pytorch_training_precision: bfloat16
pytorch_weight_path:

model:
  action_horizon: 50

lr_schedule:
  warmup_steps: 1000
  peak_lr: 2.5e-5
  decay_lr: 2.5e-6

optimizer:
  b1: 0.9
  b2: 0.95
  eps: 1.0e-8
  weight_decay: 6.0e-6
  clip_gradient_norm: 1.0

data:
  repo_id: /nvme2n1/dataset/00070_20260817_agx01_gzy
  assets:
    assets_dir: /nvme2n1/dataset/00070_20260817_agx01_gzy
    asset_id: "."
  adapt_to_pi: false
  delta_action_dims: [7, 7]
