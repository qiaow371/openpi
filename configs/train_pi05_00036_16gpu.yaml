# jiangsuan-16 16×PPU PyTorch FSDP。路径是 openpi_train 容器内路径。
# docker exec openpi_train bash /workspace/openpi/start_train_00036_16gpu.sh
config_name: pi05_aloha
framework: pytorch
exp_name: auto
project_name: openpi
checkpoint_base_dir: ./checkpoints
assets_base_dir: ./assets
batch_size: 1024
num_epochs: 100
seed: 42
num_workers: 4
log_interval: 100
save_every_epochs: 10
keep_period: 5000
overwrite: false
resume: false
wandb_enabled: false
fsdp_devices: 16
ema_decay: 0.99
pytorch_training_precision: bfloat16
pytorch_weight_path: /workspace/openpi/models/pi05_fixed
model:
  action_horizon: 50
lr_schedule:
  warmup_steps: 2000
  peak_lr: 1.0e-4
  decay_lr: 1.0e-5
optimizer:
  b1: 0.9
  b2: 0.95
  eps: 1.0e-8
  weight_decay: 1.0e-10
  clip_gradient_norm: 1.0
data:
  repo_id: /workspace/openpi/datasets/00036_20260804_RM_01_yj_21
  assets:
    assets_dir: /workspace/openpi/datasets/00036_20260804_RM_01_yj_21
    asset_id: "."
  default_prompt: "With both hands, pick up the yellow banana and the yellow lemon and put them into the black box; then take the green vegetables and place them into the blue box."
  prompt_from_task: true
  prompt_from_subtask: true
  adapt_to_pi: false
  delta_action_dims: [7, 7, -1, -1]
